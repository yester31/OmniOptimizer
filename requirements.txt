# Core
pyyaml>=6.0
pydantic>=2.5
rich>=13.7
numpy>=1.24
pillow>=10.0
pynvml>=11.5
pycocotools>=2.0.7

# PyTorch path (#1, #2)
torch>=2.3
torchvision>=0.18
ultralytics>=8.3

# ONNX Runtime path (#3, #4)
onnx>=1.16
onnxruntime-gpu>=1.18

# TensorRT path (#5, #6, #7)
# NOTE: Install tensorrt via NVIDIA's Python wheel index or inside the Docker image.
# tensorrt>=10.0
polygraphy>=0.49
