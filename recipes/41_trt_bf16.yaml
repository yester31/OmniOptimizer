name: trt_bf16
model:
  family: yolo26
  variant: n
  weights: yolo26n.pt
  ultralytics_version: null
runtime:
  engine: tensorrt
  version: null
  # Wave 14 A2: TRT BF16 on Ampere sm_80+ (BuilderFlag.BF16).
  # Shares FP32 exponent range — overflow-sensitive weights fare better
  # than FP16 at similar throughput.
  dtype: bf16
technique:
  name: none
hardware:
  gpu: null
  cuda: null
  driver: null
measurement:
  dataset: coco_val2017
  num_images: 500
  warmup_iters: 100
  measure_iters: 100
  batch_sizes: [1, 8]
  input_size: 640
  gpu_clock_lock: true
  seed: 42
constraints:
  # BF16 is primarily an accuracy axis; 0.5%p drop caps the trade-off.
  max_map_drop_pct: 0.5
  min_fps_bs1: 30
