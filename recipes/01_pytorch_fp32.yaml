name: pytorch_fp32
model:
  family: yolo26
  variant: n
  weights: yolo26n.pt         # ultralytics auto-downloads or local path
  ultralytics_version: null   # filled at runtime
runtime:
  engine: pytorch
  mode: eager                 # eager | compile
  dtype: fp32
technique:
  name: none
hardware:
  gpu: null                   # filled at runtime
  cuda: null
  driver: null
measurement:
  dataset: coco_val2017
  num_images: 500
  warmup_iters: 100
  measure_iters: 100
  batch_sizes: [1, 8]
  input_size: 640
  gpu_clock_lock: true
  seed: 42
constraints:
  max_map_drop_pct: null      # baseline sets the reference; drop is 0
  min_fps_bs1: null
